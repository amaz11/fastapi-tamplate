# Optional: copy to .env when running docker compose with env_file
# Default DATABASE_URL is already set in docker-compose.yml

APP_NAME=FastAPI Template
SECRET_KEY=change-me-in-production
DATABASE_URL=mysql+pymysql://fastapi:fastapi@db:3306/fastapi_db
LOG_LEVEL=INFO
ENABLE_DEMO_MIDDLEWARE=false
